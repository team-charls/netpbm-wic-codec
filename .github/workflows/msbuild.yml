# Copyright (c) Victor Derks.
# SPDX-License-Identifier: MIT

name: Build and test

on: [push]

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: .

jobs:
  build:
    runs-on: windows-2022
    strategy:
      matrix:
        buildPlatform: [x64]
        buildConfiguration: [Debug, Release]

    steps:
    - uses: actions/checkout@v2

    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Setup VSTest path
      uses: darenm/Setup-VSTest@v1

    - name: Restore NuGet packages
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: nuget restore ${{env.SOLUTION_FILE_PATH}}

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: msbuild /v:m /m /p:Configuration=${{matrix.buildConfiguration}} /p:Platform=${{matrix.buildPlatform}} ${{env.SOLUTION_FILE_PATH}}

    - name: Test
      working-directory: .\bin\${{matrix.buildPlatform}}\${{matrix.buildConfiguration}}\
      run: vstest.console.exe /Platform:${{matrix.buildPlatform}} .\bin\${{matrix.buildPlatform}}\${{matrix.buildConfiguration}}\test.dll
